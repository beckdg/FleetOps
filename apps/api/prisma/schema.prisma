generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

// Models will be added as domain modules are implemented.
//
// Seed data: prisma/seed.ts
// Run with:    pnpm --filter @fleetops/api prisma:seed
// Or via CLI:  pnpm --filter @fleetops/api exec prisma db seed
